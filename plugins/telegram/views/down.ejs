[Down] Check "<%= check.name %>" just went down
On <%= moment(checkEvent.timestamp).format('LLLL') %>,
a test on URL "<%= check.url %>" failed with the following error:

    <%= checkEvent.details %>

<% include details %>

WebAppMonitor won't send anymore notifications about this check until it goes back up.
<% include footer %>